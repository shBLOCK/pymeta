# Python API
This chapter covers things available to use in PyMeta Python code.

## `[py] class Token:`
The base class of classes that represent Rust tokens.
### def join(self, items: Iterable[CoerceToTokens]) -> Tokens: